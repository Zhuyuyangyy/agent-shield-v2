# AgentShield V3 技术说明

> **基于因果推断与反事实干预的 Agent 行为链治理系统**  
> **AgentShield V3.0 Layer 1-2-3**  
> commit:  | date: 2026-05-07

---

## 1. V3 定位

V1/V2 解决了"单次输出审计"和"单次工具调用影子预演"的问题。  
V3 在此基础上，将 AgentShield 升级为：

> **可推断风险根源、可推演干预后果、可治理行为演化的因果世界治理系统。**

不是"这个调用危不危险"，而是：
- 哪个 Agent 的哪次调用导致了系统风险？
- 风险沿调用链如何扩散？
- 如果提前阻断，后果会不会更好？
- 系统该如何干预，未来才能更安全？

---

## 2. V1 -> V2 -> V3 能力演进

| 维度 | V1 | V2 | V3 |
|------|----|----|-----|
| 审计对象 | AI 输出文本 | 单次工具调用 | 多 Agent 调用链 |
| 核心能力 | 幻觉检测 + RAG 溯源 | 影子预演 + 熔断拦截 | 因果推断 + 治理干预 + 反事实推演 |
| 风险观 | 静态 | 单次动态 | 链路动态 |
| 回答问题 | 这个回答对不对？ | 这次工具调用有没有风险？ | 风险从哪来、如何扩散、该不该干预？ |

---

## 3. V3 三层架构



---

## 4. Layer 1: Agent Behavior Graph

### 4.1 核心数据结构

BehaviorNode (行为节点):


BehaviorEdge (行为边):


### 4.2 Demo 场景图谱



---

## 5. Layer 2: Causality Engine

### 5.1 风险传播机制

风险沿调用链逆向传播 (从叶子到根)，衰减系数 0.5:



### 5.2 关键因果链识别

对每条从根到叶子的路径计算:
- **amplification_factor**: 风险沿路径的放大倍数
- **weak_links**: 应该被干预却放过的薄弱节点
- **risk_trajectory**: 沿路径的风险分变化序列

### 5.3 Root Cause 归因

Demo 归因结果 (Top 3):



---

## 6. Layer 3: Governance Engine

### 6.1 治理策略表 (硬编码 Demo 逻辑)

| 条件 | 动作 | 说明 |
|------|------|------|
| shadow_risk_score >= 0.90 | BLOCK | 直接阻断 |
| inherited_risk >= 0.50 且 chain_len > 0 | BLOCK | 阻断整条链 |
| downstream_risk_amplified 且 inherited >= 0.30 | DEGRADE | 降权该 Agent |
| amplification_factor >= 2.0 | UPDATE_RULES | 触发规则自动更新 |
| 其他 | ALLOW | 记录审计日志后放行 |

### 6.2 Demo 治理决策 (3条)



综合治理结论: **HIGH** (阻断 1 个高风险节点，生成 2 个反事实场景，1 个建议采纳)

### 6.3 反事实推演 (What-if Scenarios)

Demo What-if 反事实场景:



---

## 7. V3 完整链路



---

## 8. V3 核心创新点

### 创新点 1: 多 Agent 行为链建模

**现有技术问题**: 现有 Agent 安全方案只关注单次工具调用，无法感知 Agent 间协作产生的系统性风险。

**V3 方案**: 将多 Agent 调用序列建模为有向图，追踪风险沿调用链的扩散过程。

### 创新点 2: 因果推断与 Root Cause 归因

**现有技术问题**: 无法回答"哪个 Agent 导致系统失控"。

**V3 方案**: 通过逆向风险传播 + 关键因果链识别 + 责任归因计算，精确定位风险根源。

### 创新点 3: 反事实风险干预推演

**现有技术问题**: 治理决策只能事后验证，无法事前评估干预效果。

**V3 方案**: 基于 What-if Scenario 生成假设性干预路径，预测不同治理动作下的风险变化。

### 创新点 4: 从"拦截"到"治理"的范式升级

**现有技术问题**: 传统风控只有"放行/阻断"二元决策。

**V3 方案**: 完整的治理动作谱系 (BLOCK/DEGRADE/FORCE_EVIDENCE/ESCALATE_AUDIT/UPDATE_RULES)，并通过反事实推演选择最优干预路径。

---

## 9. 版本状态

| Version | Capability | Status |
|---------|------------|--------|
| V1 | AI输出合规审计：幻觉检测、RAG溯源、风险熵评分、硬门控 | Frozen |
| V2 | 数据库工具调用影子预演与熔断拦截 | Baseline committed |
| **V3** | **多Agent行为链因果推断 + 治理干预 + 反事实推演** | **Alpha (this commit)** |

**核心表述**: AgentShield V3 在 V2 单次工具调用影子预演基础上，进一步将多 Agent 调用序列建模为因果行为图谱，通过因果推断识别风险根源与扩散路径，并以治理引擎执行风险干预与反事实推演，实现了从"单次审计"到"链路治理"的能力跨越。

---

## 10. 下一步 (V3.1+)

- V2 审计引擎与 V3 图谱的自动联动 (每次 audit() 自动 add_tool_call)
- 前端行为图谱可视化 (节点-边渲染)
- 半自动规则演化机制 (record_outcome 反馈回路)
- 扩展到文件操作、网络请求工具链审计
- What-if 场景自动生成逻辑优化 (当前基于启发式，可升级为 LLM 生成)
